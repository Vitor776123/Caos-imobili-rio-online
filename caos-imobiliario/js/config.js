/* Configuração do cliente. Para jogar online apontando para um servidor hospedado
   (Render, Railway, Fly.io…), coloque aqui o endereço WebSocket dele, ex.:
     serverUrl: 'wss://caos-imobiliario.onrender.com'
   Vazio = automático: se a página foi servida pelo próprio servidor Node, usa o mesmo
   endereço; se está em file:// ou num host estático, usa o último endereço digitado
   na tela (ou ws://localhost:8080). */
window.CAOS_CONFIG = { serverUrl: '' };
